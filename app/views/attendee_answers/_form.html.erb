<%= form_for(@attendee_answer) do |f| %>
  <% if @attendee_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendee_answer.errors.count, "error") %> prohibited this attendee_answer from being saved:</h2>

      <ul>
      <% @attendee_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.text_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_area :answer %>
  </div>
  <div class="field">
    <%= f.label :attendee_id %><br />
    <%= f.text_field :attendee_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
